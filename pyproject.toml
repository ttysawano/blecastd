# SPDX-License-Identifier: MIT

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "blecastd"
version = "0.1.0"
description = "BLE Advertising beacon daemon"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
